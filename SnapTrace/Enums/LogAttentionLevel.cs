namespace SnapTrace.Enums;

/// <summary>
/// Indicates the attention level or priority that should be given to a log entry.
/// </summary>
public enum LogAttentionLevel
{
    None = 0,
    Low = 1,
    Medium = 2,
    High = 3,
    Critical = 4
}